# 数据合并功能设计文档

## 概述

本文档描述了在 tabs-box-compare 项目中实现的完整数据合并功能。该功能允许用户上传两份数据文件，通过直观的UI界面进行数据比较、冲突解决和最终合并操作。

## 核心功能特性

### 1. 合并模式切换

- **激活方式**: 点击"合并模式"按钮
- **视觉反馈**: FileOperator 界面显示蓝色边框和背景提示
- **状态标识**: 激活区域显示"激活"标签
- **退出机制**: 点击"退出合并模式"按钮恢复正常状态

### 2. 冲突检测与可视化

#### 冲突类型分类

~~- 🔴 **完全冲突**: 两个文件中存在相同名称的项目~~

- 🟡 **部分冲突**: 存在相似但不完全相同的项目，名字或者id相同且名字不为undefined
- 🟢 **无冲突**: 独一无二的项目

#### 冲突统计面板

- 实时显示各类冲突数量
- 黄色背景醒目提示
- 帮助用户快速了解数据状态

### 3. 交互操作按钮

#### 迁移操作 (→)

- **功能**: 将选中项目从一侧移动到另一侧
- **行为**: 原位置删除，目标位置添加
- **层级处理**: 支持Collection、Window、Tab三个层级
- **数据完整性**: 深拷贝避免引用问题
- 如果collection.conflict为true，则直接将最小项目（数组）添加到右侧同名/id的collection以及window内

#### 复制操作 (⧈)

- **功能**: 将选中项目复制到另一侧
- **行为**: 保留原位置，目标位置添加副本
- **重复检查**: 避免同一项目重复添加
- **智能分组**: 按层级分组处理

#### 删除操作 (×)

- **功能**: 永久删除选中的项目
- **层级支持**: 支持三个层级的删除操作
- **即时更新**: 删除后立即重新计算冲突状态
- **视觉反馈**: 删除项显示灰色样式

### 4. 智能激活机制

#### 多文件支持

- **双FileOperator**: 左右两个独立操作区域
- **选择隔离**: 每个区域维护独立的选择状态
- **激活指示**: 当前激活区域高亮显示
- **切换管理**: 点击标题区域切换激活状态

#### 状态同步

- **全局Store**: 使用Pinia管理选择状态
- **实时更新**: 选择状态变化立即反映在UI上
- **层级选择**: 支持父子关系的联动选择

## 技术实现细节

### 组件架构

#### FileComparisonPage.vue

- **角色**: 主控制器，管理全局状态和数据处理
- **功能**:
  - 文件上传处理
  - 冲突检测算法
  - 迁移/复制/删除逻辑
  - 合并数据导出

#### FileOperator.vue

- **角色**: 数据展示和操作界面
- **功能**:
  - 激活状态管理
  - 操作按钮控制
  - 冲突统计显示
  - 选择状态同步

#### Tree.vue

- **角色**: 层级数据展示
- **功能**:
  - 树形结构渲染
  - 冲突状态颜色编码
  - 父子关系维护
  - 展开/折叠控制

#### Checkbox.vue

- **角色**: 选择交互组件
- **功能**:
  - 与Store集成
  - 层级联动选择
  - 状态一致性维护
  - indeterminate状态支持

### 数据流程

```
文件上传 → 数据格式化 → 冲突检测 → UI显示
    ↓
用户操作（选择/迁移/复制/删除） → 数据更新 → 冲突重算 → UI刷新
    ↓
完成操作 → 生成合并数据 → 文件下载
```

### 冲突检测算法

```typescript
// 按标题分组检测冲突
const groups = {}
allItems.forEach((item) => {
  const key = item.title || "undefined"
  if (!groups[key]) groups[key] = []
  groups[key].push(item)
})

// 根据分组数量确定冲突状态
Object.values(groups).forEach((group) => {
  if (group.length > 1) {
    // 冲突状态
  } else {
    // 唯一状态
  }
})
```

## 使用流程

### 标准操作流程

1. **文件上传**
   - 左侧上传第一个JSON文件
   - 右侧上传第二个JSON文件
   - 系统自动格式化数据结构

2. **数据比较**
   - 点击"比较文件"按钮
   - 系统分析数据差异
   - 准备冲突信息

3. **激活合并模式**
   - 点击"合并模式"按钮
   - 界面切换到合并视图
   - 显示冲突标记和统计

4. **选择操作区域**
   - 点击任一FileOperator标题区域
   - 激活目标操作区域
   - 显示高亮和"激活"标识

5. **执行合并操作**
   - 选择需要处理的项目
   - 使用迁移(→)、复制(⧈)、删除(×)按钮
   - 实时查看冲突状态变化

6. **导出最终结果**
   - 点击"导出合并结果"按钮
   - 系统生成合并的JSON文件
   - 自动下载到本地

### 高级使用技巧

#### 批量操作

- 选择Collection级别自动选择所有子项
- 支持跨层级的多选操作
- 批量迁移保持数据结构完整性

#### 冲突解决策略

- 优先保留更新时间较新的数据
- 手动选择重要信息
- 合并而非删除重要数据

#### 数据验证

- 导出前检查数据完整性
- 验证层级关系正确性
- 确认没有重复或冲突项

## UI设计原则

### 视觉层次

- **主次分明**: 激活区域高亮，其他区域淡化
- **颜色编码**: 红、黄、绿三色清晰标识冲突类型
- **状态反馈**: 按钮状态根据选择情况动态变化

### 交互体验

- **直观操作**: 图标和文字结合，功能一目了然
- **即时反馈**: 操作结果立即反映在界面上
- **错误预防**: 按钮禁用状态防止无效操作

### 响应式设计

- **自适应布局**: 支持不同屏幕尺寸
- **滚动优化**: 长列表支持虚拟滚动
- **性能优化**: 大数据量下的流畅操作

## 性能优化

### 数据处理

- **深拷贝**: 避免引用问题导致的数据污染
- **批量更新**: 减少频繁的DOM更新
- **异步处理**: 复杂操作使用异步避免界面卡顿

### 内存管理

- **及时清理**: 不再使用的数据及时释放
- **引用管理**: 避免循环引用导致内存泄漏
- **状态重置**: 切换模式时清理无关状态

## 扩展性考虑

### 功能扩展

- **多文件合并**: 支持多于两个文件的合并
- **智能建议**: AI辅助的合并建议
- **历史记录**: 保存合并操作历史

### 数据格式

- **格式支持**: 扩展支持更多数据格式
- **导入导出**: 支持更多导出格式
- **数据转换**: 格式间智能转换

## 总结

本数据合并功能提供了一个完整、直观、高效的解决方案，让用户能够轻松处理复杂的数据合并任务。通过精心设计的UI和完善的交互逻辑，用户可以：

- **直观查看**数据差异和冲突
- **灵活操作**各种层级的数据项
- **实时反馈**操作结果和状态变化
- **安全导出**准确合并的最终数据

该设计方案充分考虑了用户体验、技术实现和未来扩展，为项目的后续发展奠定了坚实的基础。

